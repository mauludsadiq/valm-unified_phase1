systems:
  capability:
    enabled: true
    allow_stub: true
    repo_path: "../llm_nature"
    import:
      - module: "llm_nature"
        symbols: ["__version__"]

  specialization:
    enabled: true
    allow_stub: true
    repo_path: "../smallchat-sft-dpo"
    import:
      - module: "smallchat"
        symbols: ["__version__"]

  adaptation:
    enabled: true
    allow_stub: true
    repo_path: "../surprise_ttt_repo"
    import:
      - module: "surprise_ttt"
        symbols: ["__version__"]

  verification:
    enabled: true
    allow_stub: true
    repo_path: "../cbench_repo"
    import:
      - module: "cbench"
        symbols: ["__version__"]
